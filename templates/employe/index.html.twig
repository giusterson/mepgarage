{% extends 'base.html.twig' %}

{% block title %}Hello EmployeController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Espace réservé à l'employé</h1>
    <button class="btn btn-primary"> <a href="{{ path('vehicule_create') }}" > Ajouter un véhicule en vente </a>  </button>
    <button class="btn btn-primary"> <a href="{{ path('contact_create_from_employe') }}"> Ajouter un avis client </a> </button>
    <button class="btn btn-primary"> <a href="{{ path('app_register') }}"> Créer un compte client </a> </button>

   
</div>
{% endblock %}
